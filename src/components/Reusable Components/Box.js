import styled from 'styled-components';
import {
  space,
  layout,
  flex,
  typography,
  flexbox,
  color,
  border,
} from 'styled-system';

export const Box = styled('div')(
  space,
  layout,
  flex,
  typography,
  flexbox,
  color,
  border
);
